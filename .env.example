# Für lokale Entwicklung mit Cloudflare Pages dev:
# Der API-Key wird in wrangler/Cloudflare als Secret gesetzt (nicht hier)

# Optional: Überschreibe den API-Endpunkt für lokale Tests
# VITE_API_URL=/api/generate
